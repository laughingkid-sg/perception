{
  "name": "trading-course",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "python3 -m unittest discover -s tests -v && node scripts/audit-course.mjs && node --check course-ui.js"
  },
  "devDependencies": {
    "vite": "8.3.0"
  }
}
